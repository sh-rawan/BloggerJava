spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.data.rest.base-path=/api

# tells the jpa that create the table automatically if it does not exist
# validate update create-drop
spring.jpa.hibernate.ddl-auto=update


# spring.security.user.name=${WEB_USER}
# spring.security.user.password=${WEB_PASSWORD}
# spring.security.user.roles=${WEB_ROLES}
